initSidebarItems({"struct":[["Matrix","A matrix! In particular, a matrix with values in F_p. The way we store matrices means it is easier to perform row operations than column operations, and the way we use matrices means we want our matrices to act on the right. Hence we think of vectors as row vectors."],["QuasiInverse","Given a matrix M, a quasi-inverse Q is a map from the co-domain to the domain such that xQM = x for all x in the image (recall our matrices act on the right)."],["Subspace","A subspace of a vector space. # Fields  * `matrix` - A matrix in reduced row echelon, whose number of columns is the dimension of the  ambient space and each row is a basis vector of the subspace.  * `column_to_pivot_row` - If the column is a pivot column, the entry is the row the pivot  corresponds to. If the column is not a pivot column, this is some negative number — not  necessarily -1!"]]});