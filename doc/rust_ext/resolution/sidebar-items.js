initSidebarItems({"fn":[["ptr_eq","Hack to compare two pointers of different types (in this case because they might have different type parameters."]],"struct":[["Cocycle",""],["Resolution","Fields * `kernels` - For each internal degree, store the kernel of the most recently calculated chain map as returned by `generate_old_kernel_and_compute_new_kernel`, to be used if we run resolve_through_degree again. * `self_` - This should be a weak reference to yourself. This can be set in the method `set_self` after constructing the resolution, and is automatically taken care of by the `construct` function in `lib`. This is useful because we need a weak reference to `self` when construction resolution homomorphisms to unit and self maps."],["SelfMap",""]],"type":[["ModuleResolution",""]]});